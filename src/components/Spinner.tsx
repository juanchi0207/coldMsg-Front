import React from 'react';

const Spinner: React.FC = () => (
  <div className="inline-block w-6 h-6 border-4 border-gray-300 border-t-blue-600 rounded-full animate-spin" />
);

export default Spinner;
